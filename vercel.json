{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd client-side && npx prisma generate && npx next build --webpack",
  "outputDirectory": "client-side/.next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/generate-analytics",
      "schedule": "0 3 * * *"
    }
  ]
}
